{
  "name": "eliheist",
  "version": "1.0.0",
  "description": "portfolio website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwind build -i ./css/tailwind.min.css -o ./css/style.css",
    "watch": "tailwind build -i ./css/tailwind.min.css -o ./css/style.css --watch"
  },
  "author": "eliHeist",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.4.7",
    "clean-css-cli": "^5.6.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.0"
  }
}
